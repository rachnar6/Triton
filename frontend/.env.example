# Same Google OAuth Client ID as the backend's GOOGLE_CLIENT_ID (public, safe to expose).
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Base URL of the backend API. Leave as-is for local dev (Vite proxies /api to :5000).
VITE_API_BASE=/api
